# V8.6.0 CP8 — Vue Source Status

Date: 2026-08-25
Status: **SOURCE PARITY CHECKPOINT — BUILD NOT VERIFIED**

## Scope completed in source

- CP1: Vue/Vite foundation, Auth, Router, Pinia, Vue Query, AppShell, light/dark theme.
- CP2: Weekly Management and Schedule.
- CP3: Registration, emergency registration, AI review and Teacher Approval.
- CP4: Class Tracking.
- CP5: Students and Accounts.
- CP6: Root Admin class/teacher/permission management through existing bridge actions.
- CP7: Statistics, History, Teacher Comments and Settings.
- CP8: Wise Owl layered mascot, page-context messages, daily quote and rotating quote pool.

No production route uses `ComingSoonPage`.

## Explicit rulings

1. **CP4 tracking layout:** the approved later user requirement overrides the older design text that mentioned desktop status lanes. The final source uses **one full-width list** with filters (registered/missing/attention/device/no-device) so the teacher can use the full page width and reveal only the status needed.
2. **Root-admin transfer:** the current `window.SupabaseService` bridge and the V8.5.3c behavior reference expose no root-admin transfer method. The Vue source therefore does **not** invent a new RPC/Edge Function or weaken root-admin protection. Root-admin transfer remains a documented parity gap until an already-authorized bridge contract is supplied.
3. **Backend authority:** no PostgreSQL schema, RLS, grants, Auth, RPC, Edge Function or AI decision contract was changed.

## Source verification

Fresh source gate after CP8:

- `npm test`: **34/34 PASS**.
- TypeScript/SFC transpile syntax scan: **78 blocks checked, 0 syntax errors**.
- Security scan: no `eval()`, `new Function`, string-evaluated timers, `unsafe-eval`, `unload`, or `beforeunload` listeners in application source.
- Realtime: one centralized authenticated `legacyApi.subscribeRealtime(...)` entrypoint in `useRealtimeInvalidation.ts`.
- `public/supabase-service.js` SHA-256 remains `c0423ec9232f180382bce7fde41f41b654df539f555df1ea3a3e4adc2d95b009`.
- `public/config.js` is intentionally not included or printed.

## Required build gates — CHƯA XÁC MINH / NOT VERIFIED

The environment still cannot complete dependency installation. `npm ping https://registry.npmjs.org/` times out / DNS previously returned `EAI_AGAIN`. The partial `node_modules` directory does not contain `vue-tsc`; the fresh command:

```text
npm run typecheck
→ sh: 1: vue-tsc: not found
→ exit 127
```

Therefore the following gates are **NOT VERIFIED** for the new CP3–CP8 source and must be run once dependencies are available:

```bash
npm ci
npm run typecheck
npm test
npm run test:unit
npm run build
```

Do not cut over production until these gates and critical role workflow smoke tests pass.

## Deployment state

- Vanilla production/rollback remains authoritative and recoverable.
- Vue source is ready for dependency-complete verification under the parallel `/vue/` deployment path.
- No configured DIST is produced at this checkpoint because a fresh Vite build cannot be proven in the current environment.
